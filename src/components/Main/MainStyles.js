import { styled } from "styled-components";

const StyledCards = styled.div`
    display: flex;
    flex-wrap: wrap;
    gap: 10px;
    justify-content: center;
    padding: 20px 0;
    
`
const StyledButton = styled.div`
    display: block;
    text-align: center;
    padding: 30px;
    align-items: center;
`
const StyledLoad = styled.div`
    display: flex;
    justify-content: center;
`
export { StyledCards, StyledButton, StyledLoad }